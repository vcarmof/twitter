<p id="notice"><%= notice %></p>

 <div class="card mb-3">
    <img src="<%= @tweet.user.image %>" class="img-thumbnail", width="200" > 
    <div class="card-body">
      <h5 class="card-title"><%= @tweet.user.name %></h5>
      <p class="card-text"><%= @tweet.content %></p>
      <p>likes  <%= @tweet.likes.count %></p>
      <p>rt <%= @tweet.retweet %></p>
      <% if @tweet.original != "" %>
          <p class="card-text text-muted"><%= @tweet.original %></p>
      <%end %>

       <% if user_signed_in? && !Like.find_by(tweet_id: @tweet.id, user_id: current_user.id)%>

        <%= link_to "like", likes_path(current_user.id, @tweet.id), :class=>"btn btn-primary" %>
    
    <%elsif user_signed_in? %>

        <%= link_to "dislike", likes_destroy_path(current_user.id, @tweet.id), method: :delete, :class=>"btn btn-danger" %>
      
    <% end %>   
      <%= link_to 'retweet', retweet_tweet_path(@tweet.id), :class=>"btn btn-info" %> <br>
    <br>
      

      <%= link_to time_ago_in_words(@tweet.created_at), :class=>"text-muted" %>
    </div>
  </div>


<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %> <br>
<br>


<h4>LIKES</h4>
<% @likes.each do |like| %>
   <div class="card mb-3">
    <img src="<%= like.user.image %>" class="img-thumbnail", width="100" > 
    <div class="card-body">
      <h5 class="card-title"><%= like.user.name%></h5>
      <p class="card-text"><%=  %></p>
  </div>
  </div>
      

<%end %>
